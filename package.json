{
  "name": "food-planner",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build:server": "node ./buildServer.js",
    "run:server": "node build/index.js",
    "start:server": "yarn build:server && yarn run:server",
    "check-types": "tsc --noEmit",
    "generate:db:types": "schemats postgres postgres://foodplanner:foodplanner@localhost/foodplanner?sslmode=disable -c -e -o ./src/server/database/models.ts"
  },
  "dependencies": {
    "@types/bluebird": "^3.5.36",
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.31",
    "@vramework/schemats": "^1.0.4",
    "bluebird": "^3.7.2",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "helmet": "^5.1.0",
    "pg-monitor": "^1.4.1",
    "pg-promise": "^10.11.1",
    "typescript": "^4.6.4"
  },
  "devDependencies": {
    "esbuild": "^0.14.39",
    "esbuild-plugin-copy": "^1.3.0"
  }
}
